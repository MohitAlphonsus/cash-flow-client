export default function Logo({ className }) {
	return (
		<p className={`text-3xl font-bold tracking-wide ${className}`}>
			<span className="text-amber-400">Cash</span>
			<span>Flow</span>
		</p>
	);
}
